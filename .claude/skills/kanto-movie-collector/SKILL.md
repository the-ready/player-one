---
name: kanto-movie-collector
description: "Collect Kanto-area movie and screening data for the next 3 months and produce data/movies.csv for the 「イベントボード」dashboard's 映画 tab. Use when the user asks to update, refresh, or regenerate the movie dashboard data, run the weekly movie collection, or rebuild movies.csv. Also use when they invoke this by name or reference the weekly movie-gathering routine. Searches a fixed roster of Kanto cinemas (シネコンチェーン、名画座・ミニシアター、野外上映・ドライブインシアター会場)、finds each theater's current new releases, revival/retrospective programs, outdoor and special screenings, and film festivals, re-verifies every previously collected screening against the previous CSV (still showing? held over? pulled? pre-sale window closed?), records a verified poster image URL (official/press sources preferred) alongside official and booking links, and grows the theater roster in data/theaters.csv itself as cinemas open and close."
---

# 関東映画収集タスク（週次）

## このタスクの目的

`data/movies.csv` を更新し、ダッシュボード「イベントボード」の「映画」タブに**今週から3ヶ月以内に観に行ける上映**を並べること。

新作ロードショーだけではない。名画座・ミニシアターのリバイバル上映、夏季の野外上映やドライブインシアター、爆音上映・オールナイトなどの特別上映、映画祭まで含めて「新作映画や、復活上映の映画の公開日や期間」を利用者が一目で把握できるようにすることがゴールである。

`/kanto-event-collector` と同じ思想（**実際に確認できた情報だけを書く。不明なら空欄にする**）をそのまま踏襲する。特に料金・上映期間・ポスター画像URLについては、確認していないものを推測で埋めることを固く禁止する。

---

## 実行時の出力について

このタスクはユーザーとの対話ではなく、システムが定期的に自動実行するものである。あいさつ・前置き・確認や提案といった会話的な文章は書かず、タスクの遂行に必要な情報（進捗のログ、最終的な集計結果など）だけを出力するようにしよう。

---

## 出力仕様

### 出力先

- **`data/movies.csv` のみ**（全件を毎回書き直す。追記ではなく完全再生成）。`index.html` はこのタスクでは編集しない
- ダッシュボードは `data/movies.csv` を Web サーバー経由で取得する前提で作られており、`file://` で直接開く運用は想定していない

> **書き出し先に注意**: ダッシュボードは `data/` 配下しか読まず、別パスへのフォールバックを意図的に持たない。リポジトリのルート直下に `movies.csv` を書いても反映されず、古い表示が残る。

- `data/theaters.csv`（映画館の名簿）も `tools/roster.py` 経由で更新する。**ただしSKILL.md（この文書）は書き換えない。** 名簿（データ）は自動更新、ルール（散文）は提案止まりという線を守り、ルールの変更提案は `docs/skill-feedback.md` に書く

### 出力方法（CSV書き出しの手順）

全件を最後にまとめてCSVに書くのではなく、調査の区切りごとにバッチで追記する。**全件を調査結果としてコンテキストに溜め込んでから最後に一括生成する、という進め方はしないこと。**

1. 調査開始前に `python3 tools/append_rows.py movies --init` を実行し、`data/movies.csv` をヘッダーだけの状態にする。**このとき前回の内容は `data/.prev/` に自動退避される**（差分検知の材料になる）
2. 5〜10件の調査が完了するごとに、結果をまとめて以下の形式でCSVに追記する

   ```bash
   python3 tools/append_rows.py movies <<'EOF'
   {"title": "...", "genre": "...", "theater": "...", ...}
   {"title": "...", "genre": "...", "theater": "...", ...}
   EOF
   ```

3. コンテキストに全件のデータを保持する必要はない。追記が終わったバッチの内容は忘れてよい（次の調査のためにコンテキストを空ける）
4. `id` はスクリプトが自動採番する。JSONに含める必要はない
5. JSONに含めない列は空文字で補完される。分かっている列だけを渡せばよい
6. 全件の調査・追記が終わったら `python3 tools/diff_data.py movies` と `python3 tools/validate_data.py` を実行する

自然なバッチの区切りの例：

- 公開予定カレンダーで1ヶ月分の新作を拾ったらまとめて書く
- 名画座・ミニシアターの定点観測リストを調べたらまとめて書く
- 調査ステップが変わる節目（新作調査→名画座調査→野外上映補完）でまとめて書く

#### 前回から変わっていない列は書き直さない（持ち越し）

劇場の座標・読み・劇場URLは、前回値から自動で補完される。**空欄のまま渡してよい。**

内容にも変更がないことを**確認できた**行は、`"_carry": "*"` を添えると `desc` `note` `official_url` `poster_url` `poster_source` なども前回値で埋まる。

```bash
python3 tools/append_rows.py movies <<'EOF'
{"title": "...", "release_date": "2026-07-31", "screening_type": "new", "genre": "anime",
 "theater": "TOHOシネマズ|109シネマズ", "area": "東京都・千代田区", "pref": "tokyo",
 "date": "2026.7.31公開", "status": "上映中", "rank": "2", "price": "一般2,000円",
 "source": "eiga.com", "url": "https://...", "_carry": "*"}
EOF
```

> **公開日・料金・前売り期間は持ち越せない。** `onsale_end` や `price_best` を `_carry` に指定するとスクリプトがエラーで落ちる。**ムビチケの販売終了日は公開日に向かって迫ってくる**ので、前回値の流用は「終わった前売りを発売中に見せる」ことになる。
>
> `poster_url` は持ち越してよいが、**前回空欄だった作品は毎回再挑戦すること**（`_carry` は空欄を埋めるだけなので、空欄のままなら自動では埋まらない）。

### CSV列（この順序・この列名を厳守）

```
id,title,kana,genre,screening_type,area,theater,theater_url,pref,release_date,end_date,date,status,rank,series_id,announced_date,is_additional,onsale_label,onsale_start,onsale_start_time,onsale_end,onsale_end_time,limited_sale,price,price_official,price_best,discount_pct,best_source,coupon_note,price_checked,price_condition,poster_url,poster_source,source,url,official_url,lat,lng,desc,note
```

全40列。列名・順序ともに厳守すること。**空欄でよい列は多いが、列そのものを省いてはいけない**（ヘッダーが1列でもずれると検証で落ちる）。

書き終えたら `python3 tools/validate_data.py` を実行し、ERROR が0件であることを確認すること。

| 列                          | 規則                                                                                                                                                                                                                                                                        |
| --------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `id`                        | 1から連番                                                                                                                                                                                                                                                                   |
| `title`                     | 正式名称。邦題・原題の別名やサイト独自の煽り文句は含めない                                                                                                                                                                                                                  |
| `genre`                     | 下記ジャンルキーから該当するもの。複数は `\|` 区切り                                                                                                                                                                                                                        |
| `screening_type`            | 下記上映形態キーから該当するもの。複数は `\|` 区切り（例：`revival\|special`）                                                                                                                                                                                              |
| `area`                      | 表示用の地名（例：`東京都・豊島区`）                                                                                                                                                                                                                                        |
| `theater`                   | 下記「`theater`列の書き方」を厳守すること。不明なら `-`                                                                                                                                                                                                                     |
| `pref`                      | `tokyo` `kanagawa` `saitama` `chiba` `ibaraki` `tochigi` `gunma` のいずれか                                                                                                                                                                                                 |
| `release_date` / `end_date` | `YYYY-MM-DD`。**不明なら必ず空欄。推測で埋めない**。新作ロードショーは終了日未定のことが多く、その場合 `end_date` は空欄でよい                                                                                                                                              |
| `date`                      | 表示用の日付文字列（例：`2026.8.1公開` `2026.8.15〜8.21`）                                                                                                                                                                                                                  |
| `status`                    | `本日が最終上映` `まもなく公開` `上映中` `前売り券発売中` のいずれか                                                                                                                                                                                                        |
| `rank`                      | `本日が最終上映`=0 / `まもなく公開`=1 / `上映中`=2 / `前売り券発売中`=3                                                                                                                                                                                                     |
| `price`                     | 実際の料金（例：`一般2,000円/大学生1,500円/高校生以下1,000円`）。不明なら `要問合せ`                                                                                                                                                                                        |
| `poster_url`                | **実在すると確認できたポスター画像への直リンクURL**（下記「ポスター画像の取り扱い」参照）。確認できなければ空欄                                                                                                                                                             |
| `poster_source`             | ポスター画像の出典（例：`公式サイト` `配給会社サイト` `TMDb` `eiga.com`）。`poster_url` が空欄ならこれも空欄                                                                                                                                                                |
| `source`                    | 情報の出典サイト名。**URLではなく、人間が読める短いサイト名を書くこと**（例：`eiga.com` `シネマトゥデイ` `公式サイト`）。ダッシュボードはこの値をボタンのラベル「{source}で上映情報を見る」にそのまま使うため、URLを書くと「https://...で上映情報を見る」と表示されてしまう |
| `url`                       | 上映スケジュール・座席予約ページなど、ユーザーが次に見るべきページ                                                                                                                                                                                                          |
| `official_url`              | 作品公式サイト。**見つからなければ空欄**（推測URLを書かない）                                                                                                                                                                                                               |
| `theater_url`               | 劇場・上映会場の公式サイト                                                                                                                                                                                                                                                  |
| `lat` / `lng`               | 劇場・会場の代表座標（小数4桁程度）。**野外上映・ドライブインシアターの臨時会場も含め、できる限り埋める**（ダッシュボードの地図検索・現在地検索がこの座標だけを頼りに動くため）。不明なら空欄                                                                               |
| `desc`                      | 100〜150字程度。**何が目玉かを具体的に書く**（下記「descの書き方」参照）                                                                                                                                                                                                    |
| `note`                      | 日時指定制、整理券制、雨天中止、字幕/吹替の別など、料金以外の実務的な注意事項。なければ空欄                                                                                                                                                                                 |

#### 新しく追加された列（以前の版には無かったもの）

| 列                                    | 規則                                                                                                                                                               |
| ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `kana`                                | 作品名の読み（ひらがな or カタカナ）。**漢字の作品名を「かな」で検索できるようにするための列**。表示には使わない。読みが自明でない作品だけでよく、全件必須ではない |
| `series_id`                           | **特集上映・映画祭のプログラム**をまとめるキー（例：`s-ozu2026`）。同じ特集の各作品／同じ作品の複数会場に同じ値を入れる。単発上映は空欄                            |
| `announced_date`                      | 公開日・上映が**発表された日** `YYYY-MM-DD`。7日以内なら画面に `NEW` バッジが出る。**分からなければ必ず空欄**                                                      |
| `is_additional`                       | **上映延長・アンコール上映**が決まった行は `1`、それ以外は空欄                                                                                                     |
| `onsale_*` / `limited_sale`           | 前売り券（ムビチケ等）の販売期間（下記「前売り券」参照）                                                                                                           |
| `price_official` 〜 `price_condition` | 料金の比較レイヤー（下記「料金」参照）。**条件付き割引は `price_condition` が必須**                                                                                |

### 前売り券（`onsale_*`）の書き方

映画の「受付期間」は前売り券の販売期間にあたる。書くと、カード表面に「前売り発売中／前売り発売前／前売り終了」と締切カウントダウンが出て、「前売りが買えるものだけ」で絞り込めるようになる。

| 列                                   | 規則                                                                                                           |
| ------------------------------------ | -------------------------------------------------------------------------------------------------------------- |
| `onsale_label`                       | 券種（例：`ムビチケ前売券（カード）` `オンライン前売券`）。売り切れ・終了なら `販売終了` `SOLD OUT`            |
| `onsale_start` / `onsale_start_time` | 販売開始日 `YYYY-MM-DD` と時刻 `HH:MM`                                                                         |
| `onsale_end` / `onsale_end_time`     | 販売終了日と時刻。**多くのムビチケは公開日の前日で販売を終える**が、作品ごとに違うので必ず販売ページで確認する |
| `limited_sale`                       | 特典付き前売り・数量限定券など（例：`特典付き 限定5,000枚`）。無ければ空欄                                     |

**時刻が書かれていないときは空欄にする。** 画面側は「開始時刻が空なら0:00、締切時刻が空なら23:59」として扱う。勝手に補うと、その時刻まで「発売前」と嘘をつくことになる。

### 料金（`price_official` 〜 `price_condition`）—— 条件を書けない割引は載せない

映画料金は「同じ席を安く買う」経路が多く、イベントと同じ形で比較できる。ただし**映画の割引はほとんどが条件付き**である点がイベントと決定的に違う。

| 列                | 規則                                                                                        |
| ----------------- | ------------------------------------------------------------------------------------------- |
| `price_official`  | 通常料金（一般1名・円・数値のみ）。例：`2000`                                               |
| `price_best`      | 実際に払える最安額（一般1名・円・数値のみ）。例：`1300`                                     |
| `discount_pct`    | 割引率（整数%）。**両方揃った時のみ**。値引き額から計算した値と2%以上ずれると検証で落ちる   |
| `best_source`     | 最安を提供している経路名。例：`ムビチケ前売券` `auマンデー` `イオンシネマ ハッピーマンデー` |
| `coupon_note`     | 配布中クーポンの説明                                                                        |
| `price_checked`   | 価格を確認した日 `YYYY-MM-DD`。**価格を1つでも書いたら必須**                                |
| `price_condition` | **適用条件。条件付きの割引を書いたら必須**                                                  |

**`price_condition` の絶対規則。**

- `au/UQ mobile会員・月曜のみ` / `毎月1日` / `20:00以降の上映回` / `会員登録が必要` / `60歳以上` のように、**誰が・いつ・どうすれば**その額になるのかを書く
- 条件が特定できない割引は、**書かずに落とす**。条件を伏せた安い価格は、値段を出さないより有害になる（「1,300円だと思って行ったら2,000円だった」を作る）
- 誰でも無条件に買える前売り券（ムビチケ一般1,500円など）は、条件が「公開前に買うこと」だけなので `公開日前日までの購入` と書く
- **レディースデイ等、特定の性別・属性を条件とする割引は載せない。** 適用できない人には無関係な情報で、そのために表示スペースを割く意味がない

### ジャンルキー

| キー        | 対象                   |
| ----------- | ---------------------- |
| `anime`     | アニメ映画             |
| `jp-live`   | 邦画（実写）           |
| `foreign`   | 洋画（実写）           |
| `doc`       | ドキュメンタリー       |
| `fam`       | ファミリー・キッズ向け |
| `tokusatsu` | 特撮・ヒーロー         |
| `music`     | 音楽・ライブ映画       |

### 上映形態キー

| キー       | 対象                                                         |
| ---------- | ------------------------------------------------------------ |
| `new`      | 新作公開（シネコン等での通常ロードショー）                   |
| `revival`  | 名画座・ミニシアターでのリバイバル上映・特集上映             |
| `outdoor`  | 野外上映・ドライブインシアター                               |
| `special`  | 特別上映（爆音上映・オールナイト・舞台挨拶付き・応援上映等） |
| `festival` | 映画祭                                                       |

### `theater` 列の書き方（重要）

新作ロードショーで特定の1館だけを書くと、「そこでしか観られない」という誤解を与える。**上映形態によって書き方を変える**こと。ダッシュボード側は、新作(チェーン名)の行は場所を表示せず**チェーン名のみ**表示し、名画座・野外上映・特別上映・映画祭の行は具体的な会場名を表示する仕様になっている。この表示仕様と矛盾しないように、以下の書き方を厳守する。

| `screening_type`                    | `theater` に書く内容                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `new`（新作・全国/広域公開）        | **上映しているシネコンチェーン名**を書く（例：`TOHOシネマズ` `109シネマズ` `イオンシネマ` `ユナイテッド・シネマ` `MOVIX` `T・ジョイ` `USシネマ` `ヒューマントラストシネマ`）。特定の1館名（例：`TOHOシネマズ日比谷`）は書かない。**複数チェーンで同時公開されている場合は `\|` 区切りで複数書いてよい**（例：`TOHOシネマズ\|109シネマズ\|イオンシネマ`）。チェーン名の表記は下記「チェーン名の表記統一」のリストの表記に厳密に合わせること（ダッシュボードが `theaters.csv` の店舗ディレクトリと突き合わせて店舗地図を表示する際、表記が一致していないと機能しないため） |
| `new`（単館・limited公開）          | その作品を上映しているのが本当に1館だけ（単館系ミニシアター限定公開など）と確認できた場合に限り、その具体的な劇場名を書く                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `revival`（名画座・リバイバル）     | 具体的な劇場名を書く（例：`新文芸坐` `早稲田松竹`）。その劇場でしか観られない一回性の企画のため                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `outdoor`（野外上映・ドライブイン） | 具体的な会場名・臨時会場名を書く                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `special`（特別上映）               | 具体的な劇場名を書く（爆音上映・オールナイト・舞台挨拶付きは特定の劇場・日時に紐づく一回性の企画のため）                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `festival`（映画祭）                | 具体的な会場名を書く                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |

`area`・`lat`/`lng` は `theater` の書き方に関わらず、代表的な上映劇場（新作なら関東での主要上映館の1つ。複数チェーンの場合はいずれか1つでよい）の座標を入れてよい。座標は地図検索・現在地検索のために必要なので、チェーン名を書く場合でも省略しない（**ダッシュボードは新作行の`area`をカード上のテキストとしては表示しないが、地図検索・県フィルタの内部処理では引き続き使うため、空欄にしないこと**）。

#### チェーン名の表記統一（`theaters.csv` と一致させる）

映画チェーンの店舗ディレクトリは `data/theaters.csv` に別途まとめてある（`chain,name,pref,area,lat,lng,url` に加えて、名簿の保守用に `status,first_seen,last_hit,hit_count,note` を持つ）。新作行の `theater` に書くチェーン名は、必ず `theaters.csv` の `chain` 列に実在する値と**完全に一致**させること（表記ゆれがあると、チェーン名をクリックしたときの店舗地図が空になる）。対象チェーンは概ね以下の通り：

```
TOHOシネマズ / イオンシネマ / 109シネマズ / MOVIX / ユナイテッド・シネマ / T・ジョイ / USシネマ / ヒューマントラストシネマ
```

`theaters.csv` に載っていない新しいチェーン名を使う場合は、`python3 tools/roster.py theaters --add` で名簿に追加したうえで、収集結果の報告にもその旨を書くこと（チェーン名の表記が名簿と一致していないと、店舗地図が空になる）。

---

## 収集ルール

### 1. 対象期間は「直近3ヶ月のみ」

実行日から3ヶ月後までに**公開・上映されている、または公開・上映が始まる**作品のみを対象とする。

- ✅ 実行日時点で上映中のもの（新作ロードショーは終了日未定でも可）
- ✅ 3ヶ月以内に公開・上映が始まるもの
- ❌ 3ヶ月より先にしか公開・上映されないもの → **含めない**
- ❌ すでに上映が終了したもの → **含めない**

### 2. 対象地域は関東1都6県

東京都・神奈川県・埼玉県・千葉県・茨城県・栃木県・群馬県。

### 3. 目標件数

`revival`・`outdoor`・`special`・`festival` は合計で**20〜40件**を目安とする（5つの上映形態のうち `new` 以外の4種すべてから最低3件は拾うこと。ジャンルも8割が邦画実写・アニメに偏らないよう、洋画・ドキュメンタリー・特撮なども探すこと）。

**`new`（新作）には上限を設けない。** 「ステップ1：新作ロードショーの網羅調査」で拾えた対象期間内の新作は、目標件数を理由に間引かず全件載せる。結果として全体の総件数が70件を超えても構わない（件数より網羅性を優先する）。

各都県から最低2〜3件は拾い、東京に偏りすぎないようにすること（東京は全体の半分程度まで）。

---

## 調査手順

> 差分検知・持ち越し・名簿更新の考え方は `docs/COLLECTION-PROTOCOL.md` に詳しい。手を動かすのに必要なコマンドはこのSKILL.mdに全部ある。

### ステップ0：前回分の棚卸し（省略禁止）

**新規の調査を始める前に、前回載せた上映の一覧を取る。**

```bash
python3 tools/prev_rows.py movies --worklist
```

前回の52件が約4,000文字の圧縮一覧で出る。全列が必要になった行だけ、あとから引く。

```bash
python3 tools/prev_rows.py movies --uid 3f2a1b9c
python3 tools/prev_rows.py movies --venue 新文芸坐
```

映画は tier A（今回必ず再確認）の比率が高い。新作は公開日が動き、前売りは公開日で締まり、名画座の特集は1〜2週で入れ替わるためである。

この工程で確認するのは、次の4つ。

| 確認すること | 見落とすとどうなるか |
| --- | --- |
| **まだ上映しているか** | 終わった作品が「上映中」として残る。映画は終映日が告知されないことが多く、静かに終わる |
| **上映が延長されたか**（`is_additional`） | アンコール上映・ロングラン継続を拾えない |
| **前売りが終わっていないか** | 買えない前売りを「発売中」として見せる |
| **上映館が増減していないか** | 拡大公開・縮小をカードに反映できない |

> **新作行の `theater`（チェーン名）が変わっても、同じ作品として扱われる。** 差分の同定に新作の会場は使っていないため、「翌週に109シネマズが加わった」場合は [消滅]+[新規] ではなく [変更] として出る。

### ステップ1：新作ロードショーの網羅調査（最重要・省略禁止）

**新作（`screening_type=new`）は「取りこぼしなく全件拾う」ことを最優先とする。** 個別のシネコンを1館ずつ検索して見つかったものだけを載せる、というやり方は取りこぼしの原因になるため行わない。必ず**公開予定を一覧できるカレンダー・リストのページ**を起点にすること。

1. **公開予定カレンダーを月ごとに確認する。** 実行日を含む月から3ヶ月後までの各月について、以下のようなページを開き、**掲載されている作品を全部**書き出す（大作・話題作に限らず、小規模公開の作品も含めて拾う）。
   ```
   {実行年}年{実行月}月 公開映画 一覧
   {実行年}年{実行月+1}月 公開映画 一覧
   {実行年}年{実行月+2}月 公開映画 一覧
   {実行年}年{実行月+3}月 公開映画 一覧
   ```
   eiga.com・映画.com・シネマトゥデイ・Yahoo!映画などの「公開予定」「新作映画」一覧ページ、Wikipediaの「{年}年の映画」記事なども横断的に使う。
2. **主要配給会社ごとの公開予定ページも確認する。** カレンダー系サイトは掲載漏れや配給会社発表前の情報を拾えないことがあるため、東宝・東映・松竹・ワーナー ブラザース ジャパン・ディズニー（日本語）・ソニー・ピクチャーズ・ユニバーサル・ギャガ・KADOKAWA・アスミック・エース・ロングライド・ショウゲート・松竹ODS等の「公開予定作品」ページを確認する。
3. **1で拾った作品ごとに**、公開日・上映しているシネコンチェーン（`theater`列の書き方は前章の表に従う）・料金を個別に確認する。
4. **話題作・大作を取りこぼしていないか最後に必ずセルフチェックする。** 実行時点で世間的に注目されている大作・続編・話題のアニメ映画等が対象期間内にあるにもかかわらず一覧に入っていない、という状態が起きていないかを確認すること。**「目標件数に達したから打ち切る」という理由で新作の掘り起こしを止めない。** 新作は目標件数の上限にかかわらず、対象期間内に公開されるとわかっているものは可能な限り全件載せる。

参考：主要シネコンチェーン（関東主要館の例）：
TOHOシネマズ（日比谷・新宿・六本木ヒルズ・池袋等） / イオンシネマ（市原・幕張新都心等） / ユナイテッド・シネマ（としまえん・豊洲等） / 109シネマズ（プレミアム新宿・グランベリーパーク等） / MOVIX（さいたま等） / T・ジョイ（PRINCE品川等） / 川崎チネチッタ / 横浜ブルク13 / 新宿ピカデリー / 丸の内ピカデリー / グランドシネマサンシャイン池袋 / 立川シネマシティ

### ステップ2：名画座・ミニシアターの特集上映調査

定点観測する劇場は `data/theaters.csv` にある。

```bash
python3 tools/roster.py theaters --list --pref tokyo
```

**1館につき1回、上映スケジュールを調べる。そのとき「前回分の照合」と「新規の発見」を同時に済ませる。** 名画座の特集は1〜2週で入れ替わるので、前回の行はほぼ入れ替わっているはずだが、**入れ替わったことを確認せずに落としてはいけない**（延長されている場合がある）。

```
{劇場名} 上映スケジュール {実行年}年{実行月}月
{劇場名} 特集上映
```

収穫があった劇場、閉館した劇場、新しく見つけた劇場は、その場で名簿に反映する。ミニシアターは閉館・開館が多い。

```bash
python3 tools/roster.py theaters --hit 新文芸坐
python3 tools/roster.py theaters --retire ○○シネマ --reason "2026年5月閉館"
python3 tools/roster.py theaters --add <<'EOF'
{"chain": "", "name": "○○座", "pref": "tokyo", "area": "東京都・中野区",
 "lat": "35.70", "lng": "139.66", "url": "https://..."}
EOF
```

`chain` はシネコンチェーンの店舗なら親チェーン名、名画座・単館なら空欄にする（`theaters.csv` はチェーンの店舗ディレクトリと名画座の名簿を兼ねている）。**収集元のページの文言を `note` にそのまま持ち込まないこと**（`roster.py` が指示文めいた文字列を拒否するが、そもそも入れない）。

### ステップ3：野外上映・ドライブインシアター・特別上映の補完調査

施設リストに載らない単発の上映イベントを、季節キーワードと横断サイトで補完する。

```
関東 野外上映 {実行年}年{実行月}月
ドライブインシアター 関東 {実行年}
{映画タイトル} 爆音上映
{映画タイトル} オールナイト上映
映画祭 関東 {実行年}
```

補完用の横断サイト：eiga.com / 映画.com / Filmarks / シネマトゥデイ / ぴあ映画生活 / Yahoo!映画

### ステップ4：各作品・上映の深掘り

拾った作品・上映1件ごとに、**公式サイトや劇場公式サイトまで辿って**以下を確定させる。まとめサイトの記載だけで済ませない（日付・料金が古いことがある）。

1. **正確な公開日・上映期間**
2. **何が目玉か** — あらすじではなく、特集の切り口・上映形態の特徴（二本立て、爆音、日時指定制など）
3. **料金** — 券種ごとの正確な金額
4. **公式サイトURL・劇場URL**
5. **ポスター画像URL**（次章参照）

前回から続いている上映については、**変わりうる項目（上映継続の有無・上映館・前売りの締切）だけ**を確認すればよい。目玉・座標・ポスターを毎回調べ直す必要はない（`_carry` で持ち越す）。

### ステップ5：消えた行の説明と、差分の確定

```bash
python3 tools/diff_data.py movies --allow-unexplained
```

「消滅」として挙がった行は、必ず次のどちらかである。

1. 上映が終わった／公開が延期・中止になった → 正しく消えている
2. 今回は確認できなかった → **上映中の作品を一覧から落とした事故**

**映画は終映日が告知されないまま静かに終わることが多い**ので、「消えたから終わったのだろう」という推測が特に通りやすい。だからこそ1件ずつ確認して記録する。

```bash
python3 tools/prev_rows.py movies --dispose <<'EOF'
{"uid": "3f2a1b9c", "status": "ended",        "note": "劇場の上映スケジュールから外れたことを確認"}
{"uid": "8d7e6f50", "status": "cancelled",    "note": "公開延期（配給発表）"}
{"uid": "a1b2c3d4", "status": "out-of-scope", "note": "公開が3ヶ月より先に延びた"}
{"uid": "b2c3d4e5", "status": "notfound",     "note": "上映館を特定できず"}
EOF
```

最後に名簿を整理し、差分と検証を確定させる。

```bash
python3 tools/roster.py theaters --gc
python3 tools/diff_data.py movies       # 説明漏れがあると落ちる
python3 tools/validate_data.py          # ERROR 0 を確認
```

`diff_data.py` の出力が、そのまま報告の「前回からの変化」になる。**記憶や印象で変化を書かないこと。**

---

## ポスター画像の取り扱い（このタスクで特に注意すること）

### 取得元の優先順位

1. **作品公式サイト・配給会社サイトのポスター画像/OGP画像**（プロモーション目的で公開されており、作品名・リンク付きでの紹介利用は通常想定内）
2. **The Movie Database (TMDb, themoviedb.org)** のポスター画像（二次利用前提のオープンデータベース。ただし単館系・自主制作・公開直後の作品は未収録のことがある）
3. **eiga.com・映画.com・Filmarks等の映画情報サイトの画像**（上記で見つからない場合の補助。独自スキャン画像でホットリンクを拒否される場合があるため、あくまで補助的に扱う）

転売・二次流通・出典不明のSNS画像は使わない。

### 絶対規則

> **実際に画像として存在すると確認できたURLだけを `poster_url` に書く。確認できなければ空欄にする。**

- ❌ 「公式サイトにありそうだから」という推測でURLを組み立てない
- ❌ サムネイル一覧の縮小版URLをそのまま貼らない（可能なら実サイズの画像を確認する）
- ✅ 確認できなければ空欄にしてよい（ダッシュボード側がチケット風のプレースホルダーを自動表示する）

`poster_url` を記入した行には、必ず `poster_source`（出典）も記入すること。

---

## `desc` の書き方

100〜150字程度で、**その上映の何が特別なのかを具体的に**書く。

### 悪い例（一般論・中身なし）

> ❌ 話題の新作映画です。ぜひご覧ください。
> ❌ 名作を大きなスクリーンで楽しめます。

### 良い例（目玉が具体的）

> ✅ 一週間限定の二本立て特集で、監督デビュー作と最新作を続けて上映。トークイベントは初日のみ、要事前予約。
> ✅ 屋外駐車場を使ったドライブインシアター企画。車内から鑑賞するため冷房いらずで、荒天時は中止・順延の判断が当日17時に告知される。

### 含めるべき要素

- **目玉**（特集の切り口、上映形態の特徴、規模感）
- **注意事項**（日時指定制、事前予約必須、雨天中止など）

### 含めないもの

- 配給会社の宣伝文句をそのまま転記したもの
- 「必見」「感動」といった評価語
- 出典サイトの文章のコピー（**必ず自分の言葉で書き直す**）

---

## 品質チェック（提出前に必ず実施）

### 差分・名簿

- [ ] `python3 tools/diff_data.py movies` が**終了コード0**で通るか（説明のない消滅が0件か）
- [ ] 「消滅」の各行に付けた理由は、実際に劇場のスケジュールを確認した結果か（「消えたから終わったのだろう」で `ended` にしていないか）
- [ ] 「表記が変わった可能性」に挙がったペアを確認し、同じ作品なら `renamed` を記録したか
- [ ] **上映延長・アンコール上映を `is_additional=1` として拾えているか**（差分の [変更] で `end_date` が伸びた行）
- [ ] `_carry: "*"` を使った行は、内容に変更がないことを**実際に確認した**行だけか
- [ ] 収穫のあった劇場に `roster.py theaters --hit` を記録したか
- [ ] `roster.py theaters --gc` を実行したか
- [ ] 閉館したミニシアター・新しく見つけた劇場を名簿に反映したか
- [ ] ルールを変えるべきだと気づいた点を、SKILL.md ではなく `docs/skill-feedback.md` に書いたか

### 形式・内容

- [ ] 全行の列数が40列で揃っているか（`python3 tools/validate_data.py` で確認。ERRORは0件必須）
- [ ] `price_best` / `discount_pct` を書いた行すべてに `price_condition` と `best_source` と `price_checked` があるか
- [ ] **条件を特定できない割引を書いていないか**（書けないなら価格ごと落とす）
- [ ] `onsale_*` を書いた行で、時刻を推測で補っていないか
- [ ] `series_id` を付けた行が2件以上あるか（1件だけのシリーズは付けない）
- [ ] `id` が1から連番になっているか
- [ ] `release_date` / `end_date` が `YYYY-MM-DD` 形式か、または空欄か
- [ ] 上映が実行日より前に終了している作品が混ざっていないか
- [ ] 公開・上映開始が実行日の3ヶ月後より後の作品が混ざっていないか
- [ ] `pref` が7県のいずれかになっているか
- [ ] `genre` が定義済み7キーのみか
- [ ] `screening_type` が定義済み5キーのみか
- [ ] `status` が定義済み4種類のみで、`rank` と整合しているか
- [ ] `url` が実在するページか（リンク切れがないか）
- [ ] `poster_url` を書いた行すべてに `poster_source` があるか
- [ ] **確認できていないポスターURL・料金・日付を推測で埋めた行が1件もないか**
- [ ] `lat`/`lng` を、常設劇場だけでなく野外上映・ドライブインシアターの臨時会場についてもできる限り埋めたか
- [ ] `desc` が一般論で終わっていないか
- [ ] `new`・`revival`・`outdoor`・`special`・`festival` すべてから3件以上あるか（`new` 以外）
- [ ] 7都県すべてから2件以上あるか
- [ ] 東京の件数が全体の半分以下か
- [ ] **`theater` 列が上映形態ごとの書き方に従っているか**（`new` はチェーン名、単館限定公開の場合を除いて特定の1館名を書いていないか。`revival`/`outdoor`/`special`/`festival` は具体的な会場名になっているか）
- [ ] **`new` 行のチェーン名が `theaters.csv` の `chain` 列の表記と完全に一致しているか**（表記ゆれ・省略形がないか。複数チェーンは `\|` 区切りになっているか）
- [ ] `new` 行でも `area`/`lat`/`lng` が空欄になっていないか（表示はされないが地図検索に必要）
- [ ] **公開予定カレンダーを対象期間の全月について確認し、話題作・大作を取りこぼしていないか最終セルフチェックしたか**（目標件数に達したことを理由に新作の掘り起こしを打ち切っていないか）

---

## 実行手順まとめ

1. 実行日を確認し、対象期間（実行日 〜 3ヶ月後）を確定する
2. **`python3 tools/prev_rows.py movies --worklist` で前回分の棚卸しリストを取る**（省略禁止）
3. `python3 tools/append_rows.py movies --init`（前回分は `data/.prev/` に自動退避される）
4. **公開予定カレンダー・配給会社の公開予定ページを対象期間の全月について確認し、新作ロードショーを取りこぼしなく拾う**（ステップ1参照。個別のシネコン検索だけに頼らない）
5. `python3 tools/roster.py theaters --list` で名画座・ミニシアターを引き、**劇場ごとに「既存行の再確認」と「新規の発見」を同じ1回の調査で済ませる**。収穫があった劇場は `roster.py theaters --hit` を記録する
6. 季節キーワードと横断サイトで、野外上映・ドライブインシアター・特別上映・映画祭を補完する
7. 各作品・上映について公式情報まで辿り、期間・目玉・料金を確定する。`theater` 列は上映形態ごとの書き方（新作はチェーン名、それ以外は具体的な会場名）に従う。前回から続く行は、**変わりうる項目だけ**を確認し、残りは `_carry` で持ち越す
8. ポスター画像は優先順位に沿って実在確認できたものだけを採用する。**取得ツールがエラーになる等で確認できない場合は、無理に埋めず全件空欄のままでよい**（ダッシュボード側がプレースホルダー表示で吸収する）。**前回空欄だった作品は毎回再挑戦する**（`_carry` は空欄を埋めるだけなので、放っておくと空欄のまま残る）
9. **`python3 tools/prev_rows.py movies --dispose` で、消えた行1件ずつに理由を記録する**
10. `python3 tools/roster.py theaters --gc` で名簿を整理する
11. `python3 tools/diff_data.py movies` と `python3 tools/validate_data.py` を実行し、どちらも**終了コード0**であることを確認する
12. 最後に、以下の要点だけを簡潔に出力する（あいさつや前置き、次の提案などの会話的な文章は付けない）
   - 件数・都県別内訳・上映形態別内訳・ジャンル別内訳
   - ポスターURLを確認できた件数／空欄にした件数（空欄の場合はその理由も報告する）
   - **前回からの変化**（`diff_data.py` の出力をそのまま使う。記憶で書かない）
     - 新規公開／**上映延長・アンコール上映**／上映終了・公開延期
     - **`notfound` として残した件数**（0でないなら、その一覧も）
   - 名簿の変化（追加した劇場／`retired` にした劇場／`--gc` の結果）
   - `docs/skill-feedback.md` に書いた提案があれば、その要旨
