# 収集スキルへの改善提案（適用は人間が判断する）

## この文書の役割

週次の収集タスクは、**名簿（`data/spots.csv` `data/venues.csv` `data/theaters.csv`
`data/festivals.csv`）を自分で更新してよい**が、**スキル本文（`.claude/skills/*/SKILL.md`）
は書き換えてはならない**。理由は `docs/COLLECTION-PROTOCOL.md` 第7.1節に書いてある。

散文側を変えたほうがよいと気づいたときは、ここに追記する。適用するかどうかは人間が
判断し、適用したら「適用済み」に移す。

## 書き方

実行日の見出しの下に、次の形で1件ずつ書く。**「気づいた」だけでなく、
何を根拠にそう思ったかを書くこと。**根拠のない提案は判断できない。

```markdown
### YYYY-MM-DD /kanto-xxx-collector

- **[対象]** 変えたい箇所（SKILL.md の章名・行の内容）
  - **現状**: いま何と書いてあるか
  - **観測**: 実行中に何が起きたか（件数・失敗の内容・所要時間など、具体的に）
  - **提案**: どう変えるか
```

## 提案してよいもの / いけないもの

|                                                | 例                                                                                                                                                                                                                                                                                                       |
| ---------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **提案してよい**                               | 目標件数・カテゴリ配分が実態に合わない／参照先サイトが閉鎖した・新しい有力なサイトが現れた／季節キーワード表の更新／プレイガイド・配給会社リストの更新／CSVの列や検証ルールに実務上の無理がある／手順の順序が非効率                                                                                      |
| **提案は書いてよいが、自分で変えてはいけない** | 上記すべて（**書き換えは必ず人間が行う**）                                                                                                                                                                                                                                                               |
| **提案自体を慎重にすべきもの**                 | 「推測で埋めない」「確認していない価格・日付を書かない」「転売サイトを使わない」「画像を収集しない」といった**禁止事項の緩和**。これらは調査が難航したときに緩めたくなるが、緩めたい気持ちが生じること自体は緩めてよい理由にならない。提案するなら、緩めても利用者に実害が出ないと言える根拠を添えること |

---

## 未適用の提案

（ここに追記していく）

---

## 適用済み

### 2026-08-02 3スキル共通（この仕組みの導入時に解消した分）

- **[イベント・映画]** 前回CSVを読む手順が無いのに「前回からの主な変化」「前回から新たに出たクーポン・終了したセール」の報告を求めていた
  - **観測**: 読んでいないものとの差分は書けない。報告項目が構造的に空欄か、憶測になっていた
  - **適用**: 3スキルにステップ0（棚卸し）を新設し、`tools/diff_data.py` の出力を報告の根拠にするよう変更
- **[ライブ]** CSV列の定義が本文で32列、品質チェックで35列と食い違っていた（実データは35列）
  - **適用**: 列の並びに `parking` `nearest_station` `apple_music_url` を追記し、35列に統一
- **[イベント・映画]** 出力先が `events.csv` `movies.csv` と書かれており、`data/` が抜けていた
  - **観測**: ダッシュボードは `data/` 配下しか読まない（DESIGN 3.2）。ルート直下に書くと、更新したのに古い表示が残る
  - **適用**: `data/events.csv` `data/movies.csv` に統一し、書き出し先の注意書きを3スキルに揃えた
- **[ライブ]** 「ステップ0を `--init` より前に実行すること」という順序の制約が、人間向けの注意書きだけで守られていた
  - **観測**: 一度順序を間違えると、その週の追加公演検知は復旧不能だった
  - **適用**: `append_rows.py --init` が初期化の前に `data/.prev/` へ退避するようにし、順序の制約自体を無くした
- **[ライブ]** さいたまスーパーアリーナの休館情報が SKILL.md に直接書かれていた
  - **観測**: 散文に書いた期限つきの情報は、期限が来ても更新されない
  - **適用**: `data/venues.csv` に `closed_until` 列を設け、`roster.py --list` が休館中の会場を自動で除くようにした
- **[イベント]** 施設リスト（234件）が SKILL.md の散文に埋まっていた
  - **適用**: `data/spots.csv` に外出しし、`roster.py` で追加・昇格・整理できるようにした
